require 'test_helper'

class TokenGenerationWorkflow < ActionDispatch::IntegrationTest
  test 'the token is build from the submited credit card data' do
    skip 'todo'
  end

  test 'the token expires within the 10 minutes after its creation' do
    skip 'todo'
  end
end
